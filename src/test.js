// original array
let array = []

// object to add
let object = { optionDescription: 'jebdjhebdns ', noOfVotes: 8 }

// program to append an object to an array

function insertObject(arr, obj) {
  // append object
  arr.push(obj)

  console.log(arr)
}

// call the function
insertObject(array, object)
